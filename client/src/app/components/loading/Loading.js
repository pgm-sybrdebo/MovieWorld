import styled from 'styled-components';

const Text = styled.p`
  padding: 1rem;
`;

const Loading = () => {
    return (
        <div>
            <Text>Loading...</Text>
        </div>
    )
}

export default Loading